# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_DATABASE=dashboard_db

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Secret (CAMBIARE IN PRODUZIONE CON UNA CHIAVE SICURA!)
# Genera una chiave casuale con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend URL (per CORS)
FRONTEND_URL=http://localhost:5173

# Logging
LOG_LEVEL=debug
